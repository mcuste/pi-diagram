# Changelog

All notable changes to this project are documented in this file. The format follows
[Keep a Changelog](https://keepachangelog.com/en/1.1.0/), and this project uses
[semantic versioning](https://semver.org/spec/v2.0.0.html).

## [Unreleased]

### Added

- Project scaffold: TypeScript build, Biome formatting and linting, Knip dead-code detection,
  publint package validation, dependency audit, CI, and the tag-triggered release workflow.
- The public `diagram` tool schema, registered by the extension entry point. Rendering is not
  implemented yet, so a call reports that instead of drawing.
- Oh My Pi marketplace catalog at `.omp-plugin/marketplace.json`.
