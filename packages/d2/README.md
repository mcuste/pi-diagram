# @mcuste/pi-diagram-d2

D2 rendering pipeline for pi-diagram: safe-subset preflight, profiles, the isolated D2 CLI run, and PNG rasterization.

This package is part of [pi-diagram](https://github.com/mcuste/pi-diagram). Install the
extension with `@mcuste/pi-diagram`; this package is a dependency of it and has no separate
setup.
